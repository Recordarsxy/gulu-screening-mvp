export function commitComtreeValueInMainWorld(field,value){
 const clean=(input)=>String(input??'').replace(/\s+/g,' ').trim(),expected=clean(value);const labels={cities:['所在城市','城市','工作地点'],industries:['行业'],functions:['职能']}[field];if(!labels||!expected)return{accepted:false,field,value:expected};
 const visible=(node)=>Boolean(node&&node.getClientRects().length&&getComputedStyle(node).display!=='none'&&getComputedStyle(node).visibility!=='hidden');const root=[...document.querySelectorAll('a.search-label')].find((node)=>labels.includes(clean(node.textContent)))?.closest('li');if(!root)return{accepted:false,field,value:expected};
 const matches=[...root.querySelectorAll('.jqtree-title')].filter((node)=>visible(node)&&clean(node.textContent).includes(expected)).sort((a,b)=>{const exactA=clean(a.textContent)===expected?0:1,exactB=clean(b.textContent)===expected?0:1;if(exactA!==exactB)return exactA-exactB;return Number(a.getAttribute('aria-level')||99)-Number(b.getAttribute('aria-level')||99)});const choice=matches[0];if(!choice)return{accepted:false,field,value:expected};choice.click();
 const confirm=[...root.querySelectorAll('button')].find((node)=>visible(node)&&clean(node.textContent)==='确认');confirm?.click();const selected=clean(root.querySelector('.comtree-title span')?.textContent),hiddenValue=clean(root.querySelector('input[name=value]')?.value);if((!selected||selected==='请选择')&&!hiddenValue)return{accepted:false,field,value:expected};const add=[...root.querySelectorAll('button')].find((node)=>visible(node)&&clean(node.textContent)==='添加');if(!add)return{accepted:false,field,value:expected};add.click();return{accepted:true,field,value:expected,committed:true};
}
