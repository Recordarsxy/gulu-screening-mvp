# 谷露只读后台连接器 v1.1.0

这是 Manifest V3 扩展，只连接真实谷露 `121.43.105.7` 与本机服务 `127.0.0.1`。

在 `chrome://extensions` 开启开发者模式，选择“加载已解压的扩展程序”，然后选择本目录。配对码由网站“连接与设置”页面生成。

扩展会维护一个不激活的谷露标签页，单线程、随机间隔执行已人工确认的任务。登录失效、验证码或页面不兼容时会停止并要求人工处理。

扩展只读白名单字段，不读取联系方式、地址、照片、备注和附件，也不执行任何人才数据写入操作。
