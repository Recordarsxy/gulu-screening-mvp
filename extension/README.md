# Chrome 只读连接器

此目录目前是**默认禁用**的安全骨架，不会读取或操作真实谷露页面。网站阶段一不需要安装它。

只有同时提供以下材料并通过快照测试后，才能启用真实域名权限和内容脚本：

1. 谷露实际域名；
2. 脱敏搜索结果页截图或 HTML 快照；
3. 脱敏候选人详情页截图或 HTML 快照；
4. 公司批准的字段和浏览器扩展策略。

后续适配仍只允许读取页面可见的公司、职位和工作经历。验证码、账号警告、登录失效、页面结构变化或连续三次读取失败时必须立即暂停。
